<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BiblioUnifor">

        <!-- TELA INICIAL -->
        <activity
            android:name=".TelaRF01BemVindo"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RF02 e RF03 -->
        <activity android:name=".TelaRF02Intermediaria"/>
        <activity android:name=".TelaRF03LoginAluno"/>

        <!-- RF10 e RF11 -->
        <activity android:name=".TelaRF10Configuracao"/>
        <activity android:name=".TelaRF11RedefinirSenha"/>

        <!-- ADM -->
        <activity android:name=".TelaRF31GerenciamentoDeUsuarios"/>
        <activity android:name=".TelaRF32UsuariosParaADM"/>
        <activity android:name=".TelaRF33Solicitacoes"/>
        <activity android:name=".TelaRF34LivrosCRUD"/>
        <activity android:name=".TelaRF35CadastroDeLivros"/>

    </application>

</manifest>